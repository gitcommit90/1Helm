# 1Helm — ONE project

**Work only in `/root/1Helm`.**

That is the product. demo.1helm.com and port 8124 run this tree.
Do not use nested `.claude/worktrees/*` for day-to-day product work.

```bash
cd /root/1Helm
npm run build
# server changes:
systemctl restart 1herd-refactored.service
```

Codex: open this directory. Not a worktree. Not ReRouted.
