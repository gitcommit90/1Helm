# 1Helm x.y.z

Short outcome-first summary.

## What changed

- List the user-visible changes.

## Downloads

- `1Helm-x.y.z-arm64.dmg`
- `1Helm-x.y.z-mac-arm64.zip`
- `1Helm-x.y.z-linux-node.tgz`

Windows publishes no artifact; its fresh install uses the Linux archive through
WSL 2. Release verification is fresh install, correct version, startup, and
setup endpoint health on Linux, Windows, and Mac.
