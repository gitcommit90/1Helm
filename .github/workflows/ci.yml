name: CI

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  release-transport:
    name: release transport
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trigger retained Mac release upload"
