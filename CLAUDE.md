# 1Helm

**Single product root: `/root/1Helm`.**

| Surface | Path / port |
| --- | --- |
| Code + Codex + you | `/root/1Helm` |
| Live app | `https://demo.1helm.com` and `:8124` → this tree |
| Data | `CTRL_DATA_DIR=/root/1Helm/data-refactored` |

Nested `.claude/worktrees/` are archives only — do not develop there.

Keep `docs/VISION.md` current. VPS cold deploy: `scripts/deploy-vps-fresh.sh`.
Private repo: `gitcommit90/1Helm`.
